<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.peonline">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".teachermain.createCourse"
            android:label="@string/title_activity_create_course"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".studentmain.Enroll"
            android:label="@string/title_activity_enroll"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".studentmain.StudentMainMenu"
            android:label="@string/title_activity_student_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".teachermain.TeacherMainMenu"
            android:label="@string/title_activity_teacher_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".login.Register"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".video.PlayVideoActivity"
            android:label="play_video" />
        <activity
            android:name=".gps.GPSActivity"
            android:label="GPS Activity" />

        <service android:name=".gps.GPSService" />

        <activity
            android:name=".video.VideoSubmission"
            android:label="video_submission" />

        <meta-data android:name="com.google.android.gms.version" />

        <activity
            android:name=".login.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>